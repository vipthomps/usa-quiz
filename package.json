{
  "name": "usa-states-capitals-quiz-touch-teacher-cloudflare-ready",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "build": "echo "Static site: no build required"",
    "serve": "python -m http.server 8000"
  }
}
